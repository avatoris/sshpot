# Overrides for the bundled cowrie.cfg.dist. Cowrie merges this file on
# top of its defaults, so only keys that need to change are listed here.
#
# Note: [ssh] enabled, [telnet] enabled=false and [output_jsonlog] enabled
# are already the upstream defaults - not repeated here on purpose.

[honeypot]
hostname = prod-db-02
